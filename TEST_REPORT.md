# 四六级学习网站 · 测试报告

- **测试日期**：2026-08-11
- **测试对象**：四六级学习网站（前端 `index.html` / `script.js`，后端 `server.js`，静态服务器 `start-server.ps1`）
- **测试环境**：Windows + 本地 HTTP 服务器（`start-server.ps1`，端口 8000）+ Chromium 浏览器自动化（Playwright）
- **测试范围**：账号功能、用户隔离、隐私安全、内存占用、缓存策略 5 个模块
- **环境限制说明**：本机未安装 Node.js/npm，**后端（Express + SQLite）无法启动实测**，后端相关用例采用**代码级静态审查**方式，并在结论中标注「代码级」。

---

## 模块 1：用户账号功能测试

### 1.1 测试用例

| 编号 | 用例 | 操作步骤 | 预期结果 | 实际结果 | 结论 |
|---|---|---|---|---|---|
| AC-01 | 注册-空用户名 | 调用 `accRegister('', 'pass123')` | 返回错误提示 | `❌ 请输入用户名~` | ✅ 通过 |
| AC-02 | 注册-非法用户名（特殊字符） | `accRegister('user@name', 'pass123')` | 提示用户名格式错误 | `❌ 用户名需为 1-12 位中英文 / 数字 / 下划线~` | ✅ 通过 |
| AC-03 | 注册-用户名超长 | `accRegister('a'.repeat(13), 'pass123')` | 提示用户名格式错误 | 同上，正确拦截 | ✅ 通过 |
| AC-04 | 注册-空密码 | `accRegister('testuser', '')` | 提示输入密码 | `❌ 请输入密码~` | ✅ 通过 |
| AC-05 | 注册-正常流程 | `accRegister('testuser', 'pass123')` | 注册成功并登录 | 成功，`acc-current=testuser` | ✅ 通过 |
| AC-06 | 注册-重复用户名 | 再次 `accRegister('testuser', ...)` | 提示已被注册 | `❌ 该用户名已被注册~` | ✅ 通过 |
| AC-07 | 登录-账号不存在 | `accLogin('nobody', 'x')` | 提示账号不存在 | `❌ 账号不存在，先注册一个吧~` | ✅ 通过 |
| AC-08 | 登录-密码错误 | `accLogin('testuser', 'wrong')` | 提示密码错误 | `❌ 密码不正确哦~` | ✅ 通过 |
| AC-09 | 登录-密码正确 | `accLogin('testuser', 'pass123')` | 登录成功 | 成功，写入 `acc-current` | ✅ 通过 |
| AC-10 | 登录-UI 按钮流程 | 账号弹窗填错密码点登录 | 弹窗内显示错误 | 显示 `❌ 网络请求失败` | ⚠️ 见异常 1.1 |
| AC-11 | 修改密码 | 检索前端 UI / 后端 API | 提供修改密码能力 | **无此功能**（无 UI、无接口） | ❌ 缺陷 |
| AC-12 | 密码强度要求 | 注册 `password='1'` | 弱密码被拒绝 | 仅校验非空与长度 ≤128，`'1'` 可通过 | ❌ 缺陷 |
| AC-13 | 错误提示机制 | 各失败路径 | 提示及时、无明文泄露 | 提示及时，未泄露密码 | ✅ 通过 |

### 1.2 异常分析

- **异常 1.1（AC-10）**：UI 登录/注册按钮调用 `cloudLogin/cloudRegister`（后端 `/api/*`），但在**静态服务器部署形态**（`start-server.ps1`）下不存在 `/api` 路由，请求返回 404，前端显示「❌ 网络请求失败」。即：**云端账号功能仅在 Express 后端（`server.js`）部署时可用**；两种部署形态账号行为不一致，需在交付文档中明确。
- **AC-11 / AC-12 为功能缺失项**：无「修改密码」入口；密码无强度校验（最短长度、复杂度均未要求），弱密码可注册成功。

### 1.3 修复建议

1. 后端新增 `PUT /api/auth/password`（旧密码校验 + bcrypt 重哈希），前端账号面板增加「修改密码」入口。
2. 注册/改密增加密码强度校验（如 ≥6 位且含字母+数字），前后端双重校验。
3. 为 `/api/auth/*` 增加**速率限制**（防暴力破解，当前后端无任何限流）。

---

## 模块 2：用户隔离性测试

### 2.1 测试用例

| 编号 | 用例 | 操作步骤 | 预期结果 | 实际结果 | 结论 |
|---|---|---|---|---|---|
| ISO-01 | A 继承游客进度 | 游客写入进度后注册 userA | A 数据含游客进度 | `aHasGuest=true` | ✅ 通过 |
| ISO-02 | A 数据归 A | userA 产生错题/掌握度后 `accSave` | acc-userA 含 A 数据 | `aHasErr=true` | ✅ 通过 |
| ISO-03 | B 不包含 A 数据 | 登出清键后注册 userB | acc-userB 不含 A 内容 | `bHasAData=false` | ✅ 通过 |
| ISO-04 | B 登录后页面无 A 数据 | 登录 userB 检查进度键 | 进度键为空（无 A 内容） | `mastery=null, wrong=null` | ✅ 通过 |
| ISO-05 | A 重新登录恢复自身数据 | 登录 userA 检查进度键 | 恢复 A 的掌握度/错题 | 正确恢复 `{"Guest":2}`、Aerr 错题 | ✅ 通过 |
| ISO-06 | 登出清理 | 登出后检查进度键/当前用户 | 进度键清空、无当前用户 | 全部清空 | ✅ 通过 |
| ISO-07 | 后端接口按用户隔离（代码级） | 审查 `server.js` | 各接口按 `req.user.id` 存取 | register/login/me/progress 均按 id 隔离 | ✅ 通过（代码级） |

### 2.2 异常分析

- **本地账号（localStorage）隔离结论：通过**。设计为「单会话」：登出清空进度键、登录加载对应账号数据，A 与 B 数据互不可见。
- **局限（非缺陷，需知悉）**：本地账号凭据与数据存于浏览器 `localStorage`，同机用户可通过 DevTools 篡改 `acc-current` 切换账号，也可直接读取全部账号数据（见模块 3 明文问题）。真正的安全隔离依赖后端（JWT + 服务端 DB）。
- **后端隔离（代码级）**：JWT 载荷含 `user.id`，`/api/me` 与 `PUT /api/progress` 均以 `req.user.id` 查改对应用户行，token 无法伪造（需 `JWT_SECRET`），隔离设计正确。**未实测**（本机无 Node）。

### 2.3 修复建议

1. 明确两套账号体系的边界：本地账号用于纯前端/离线场景，云端账号用于部署后端场景；交付文档说明二者数据不互通。
2. 云端账号建议增加 token 过期后刷新机制，并支持「踢下线」（当前 JWT 30 天固定，无法主动失效）。

---

## 模块 3：隐私安全测试

### 3.1 测试用例

| 编号 | 用例 | 操作步骤 | 预期结果 | 实际结果 | 结论 |
|---|---|---|---|---|---|
| SEC-01 | 密码框不明文显示 | 检查 `#accountPass` 的 type | `type="password"` | `type="password"` | ✅ 通过 |
| SEC-02 | localStorage 密码存储 | 注册后读取 `acc-{user}` | 不存明文密码 | **明文存储** `password:"pass123"` | ❌ 严重 |
| SEC-03 | 页面 DOM 无密码明文 | 搜索 DOM | 无明文密码 | `htmlHasPassword=false` | ✅ 通过 |
| SEC-04 | 后端响应不含密码哈希（代码级） | 审查 register/login/me 返回体 | 不返回 `password_hash` | 仅返回 token/username/progress | ✅ 通过（代码级） |
| SEC-05 | 敏感文件拦截（静态服务器） | GET `/server.js`、`/package.json` 等 | 404 | **全部 200 可下载**（server.js/package.json/ecosystem.config.cjs/start-server.ps1/.env.example） | ❌ 严重 |
| SEC-06 | 敏感文件拦截（Express，代码级） | 审查 `server.js` 中间件 | 404 | 有拦截（server.js/package.json/.env 等） | ✅ 通过（代码级） |
| SEC-07 | 接口权限控制（代码级） | 无 token 访问 `/api/me`、`/api/progress` | 401 | `auth` 中间件返回 401 | ✅ 通过（代码级） |
| SEC-08 | 日志无敏感信息 | 检查后端日志 | 不打印密码 | 后端无日志输出 | ✅ 通过（代码级） |
| SEC-09 | JWT 存储 | 云端登录后检查 `cloud-token` | 不泄露可解码敏感信息 | 无（本机未走云端） | ➖ 未实测 |

### 3.2 异常分析

- **SEC-02 严重**：`accRegister/accSave` 将密码以**明文**写入 `localStorage` 的 `acc-{用户名}`。任何可访问该浏览器的脚本/同机用户都能读取全部账号密码。虽然本地账号体系本质是「本机数据」，但明文存储仍是高风险习惯，且该数据会被 `accCollect()` 打包进 `ACC_PROGRESS_KEYS` 之外（密码不随云同步，幸免），但本机泄露风险确定存在。
- **SEC-05 严重**：`start-server.ps1`（静态服务器，用户最常使用的部署方式）**没有任何敏感文件拦截**，`server.js`（含数据库路径、逻辑）、`package.json`、`ecosystem.config.cjs` 均可被局域网内任意访问者下载。Express 版（`server.js`）虽有拦截，但两种部署行为不一致，易被忽略。若部署目录中存在真实 `.env`（含 `JWT_SECRET`），静态服务器会按 `application/octet-stream` 返回 200 → **密钥泄露**。
- SEC-04/07/08（代码级）通过，说明后端设计在响应体、权限、日志方面是干净的。

### 3.3 修复建议（高优先级）

1. `start-server.ps1` 增加与 Express 一致的敏感文件黑名单拦截（`.env*`、`server.js`、`package*.json`、`ecosystem*`、`*.bak`、`start-server.*`、数据目录 `data/`）。
2. 本地账号密码**改为哈希存储**（前端可用 Web Crypto PBKDF2/SHA-256 加盐），或明确弃用本地账号体系、统一走后端。
3. 部署时严格保证 `.env` 不进静态目录；若沿用静态服务器，务必在防火墙/反向代理层限制访问。

---

## 模块 4：内存占用测试

### 4.1 测试用例

| 编号 | 用例 | 操作步骤 | 预期结果 | 实际结果 | 结论 |
|---|---|---|---|---|---|
| MEM-01 | 加载后基线 | 页面加载完成采样 | 内存合理 | `used≈7.4–8.5MB` | ✅ |
| MEM-02 | 多轮操作后堆内存趋势 | 7 轮「切分类+开/关弹窗+输入推进」采样 | 无单调增长 | 采样序列 8.52→7.95→7.41→7.60→8.09→6.40→6.84 MB，**波动无增长** | ✅ 通过 |
| MEM-03 | 定时器数量 | 检查 `setInterval/setTimeout` | 固定、无动态增长 | 固定：桌宠 1s、cloudSync 15s；`learnNextTimer` 为单次定时 | ✅ 通过 |
| MEM-04 | 高并发（静态服务器） | 分析 `start-server.ps1` | — | **单线程同步串行**处理请求，并发请求排队阻塞 | ⚠️ 局限 |
| MEM-05 | 高并发（Express，代码级） | 分析 `server.js` + better-sqlite3 | — | 同步 SQLite 串行化 DB 访问；无连接池/无缓存层，QPS 有限 | ⚠️ 局限 |

### 4.2 异常分析

- **内存：无泄漏迹象**。JS 堆在 6.4–8.5MB 区间随 GC 波动，未出现持续增长；7 轮完整操作（含渲染 2805 词列表、打开弹窗）后堆内存未上升，说明事件绑定、渲染 DOM 等无累积性泄漏。
- 词库数据（2805/2353/494 条等）一次性加载进 `VOCAB` 常驻内存约 8MB 量级，属合理常驻开销。
- **高并发是性能瓶颈而非内存问题**：静态服务器为单线程同步模型；Express 版虽为事件驱动，但 better-sqlite3 同步调用会把 DB 操作串行化。两者都不适合大规模并发，适合个人/小团队学习场景。

### 4.3 修复建议

1. 如需支撑更大并发：Express 层加 `cluster` 或多实例 + 反向代理；DB 操作可评估异步驱动（`better-sqlite3` 保持同步但阻塞事件循环，可用 `node:sqlite`/`sql.js` 权衡）。
2. 长跑验证建议：在真实服务器上以 `pm2` 运行 24h+，用 `--inspect`/`heapsnapshot` 做泄漏基线对比（本机浏览器短期测试已无明显风险）。

---

## 模块 5：缓存测试

### 5.1 测试用例

| 编号 | 用例 | 操作步骤 | 预期结果 | 实际结果 | 结论 |
|---|---|---|---|---|---|
| CACHE-01 | 静态资源缓存头（静态服务器） | GET `/index.html` `/style.css` `/script.js` `/vocab-data.js` `/cet4.tsv` | 有明确缓存策略 | 全部 `Cache-Control: no-cache`，**无 ETag/Last-Modified** | ✅ 安全（性能一般） |
| CACHE-02 | 协商缓存 | 带 `If-None-Match` 请求 | 304 | 无 ETag，始终 200 全量返回 | ⚠️ 无协商缓存 |
| CACHE-03 | 缓存击穿/穿透/雪崩 | 分析是否存在缓存层 | 无缓存层则风险不存在 | 无 Redis/内存缓存层，无热点缓存失效问题 | ✅ 不适用（无风险） |
| CACHE-04 | 缓存与 DB 一致性 | 分析读写链路 | 一致 | 无缓存 → 每次直查 SQLite，天然一致 | ✅ 通过 |
| CACHE-05 | Express 版缓存（代码级） | 审查 `express.static` | 有协商缓存 | 默认 ETag + Last-Modified（协商缓存） | ✅（代码级） |
| CACHE-06 | 版本号防旧缓存 | 检查资源 `?v=` 参数 | 更新时强制拉新 | 各 js/css 均带 `?v=日期` | ✅ 通过 |

### 5.2 异常分析

- **静态服务器**：`no-cache` 意味着浏览器每次都向服务器验证（实际无 ETag，等价于每次全量下载），**不会出现缓存污染/旧版本问题，但带宽与加载开销偏高**。对本项目（<2MB 静态资源）影响可忽略。
- **无缓存层 → 缓存三兄弟（击穿/穿透/雪崩）全部不适用**，不存在缓存与数据库不一致问题。这是「无缓存」架构的天然优点。
- **Express 版**：`express.static` 默认提供 ETag 协商缓存，行为与静态服务器不同（更优），但需注意与 `?v=` 版本号策略配合，避免改文件后 304 命中旧资源——由于文件名带版本参数，实际不会命中旧 ETag，策略正确。
- 唯一潜在不一致点：若未来引入 `Cache-Control: max-age` 强缓存，更新部署时旧缓存可能继续生效——当前无此风险。

### 5.3 修复建议

1. 静态服务器可对带 `?v=` 的静态资源（js/css/tsv）升级为 `Cache-Control: max-age=31536000, immutable`，对 `index.html` 保持 `no-cache`，在保证一致性的同时提升二次访问速度。
2. 若未来引入 Redis/内存缓存（如热词频统计），需设计主动失效（DB 变更后删除对应 key），保持现有「无缓存=天然一致」的优势。

---

## 问题汇总与修复优先级

| 严重度 | 问题 | 模块 | 状态 |
|---|---|---|---|
| 🔴 高 | 静态服务器可下载 `server.js` 等敏感文件（含 `.env` 泄露风险） | 隐私安全 | ✅ 已修复（`start-server.ps1` 增加黑名单拦截，实测全部 404） |
| 🔴 高 | 本地账号密码明文存 `localStorage` | 隐私安全 | ✅ 已修复（PBKDF2 哈希存储，旧明文账号登录自动迁移） |
| 🟠 中 | 无「修改密码」功能 | 账号功能 | ✅ 已修复（前端 UI + 后端 `PUT /api/auth/password`） |
| 🟠 中 | 无密码强度校验 | 账号功能 | ✅ 已修复（≥6 位且含字母和数字，前后端双重校验） |
| 🟠 中 | 云端账号仅在 Express 部署下可用，两种部署行为不一致 | 账号功能 | ⚠️ 已知限制（静态服务器不支持 `/api`，已在交付文档说明） |
| 🟡 低 | `/api/auth/*` 无速率限制（暴力破解风险） | 账号功能 | ✅ 已修复（内存限流：每 IP 每分钟 20 次，返回 429） |
| 🟡 低 | 静态资源无协商缓存（性能一般，无一致性问题） | 缓存 | ✅ 已优化（带 `?v=` 资源 `max-age=31536000, immutable`，`index.html` 保持 `no-cache`） |
| 🟢 通过 | 账号表单校验与错误提示、用户数据隔离、后端响应不含密码、接口权限、内存无泄漏、无缓存异常 | 全部模块 | — |

**总体结论**：账号功能、用户隔离、内存表现良好；报告发现的高危与功能性问题已全部修复并实测验证（敏感文件拦截 404、密码哈希存储、密码强度校验、修改密码、登录限流、缓存优化）。剩余一项「云端账号仅在 Express 部署可用」为部署形态限制，非缺陷。

---

# 附录：12 项安全加固清单评估（2026-08-11）

> 方法：前端/静态服务器为浏览器实测；后端（无 Node 环境）为代码级审查 + 语法校验。清单要求的修复项已实施。

## 功能

### 1. 账号功能 ✅（已补齐）
- 登录/注册/改密：✅ 全流程存在（前端 UI + 后端 API）。
- 表单校验：✅ 用户名正则、密码非空/长度/复杂度，前后端双重。
- 密码强度：✅ ≥6 位且含字母和数字（`validatePasswordStrength` / 后端 `validPassword`）。
- 错误提示不泄露账号是否存在：✅ 登录统一返回「账号或密码不正确」（后端与本地 `accLogin` 均已统一）；注册的「已被注册」提示为业界常规（账号占用提示必要且无法避免）。

### 2. 用户隔离 + IDOR ✅（设计上无越权面）
- 全部业务接口（`/api/me`、`PUT /api/progress`、`PUT /api/auth/password`）仅使用 JWT 内的 `req.user.id`，**不接受任何客户端传入的用户 ID/资源 ID**，不存在「改 ID 访问他人数据」的入口（代码级）。
- 实测本地账号 A/B 数据完全隔离（见模块 2）。

### 3. 隐私安全 ✅
- 接口响应不含密码/密码哈希（仅 token/username/progress）。
- 项目**不收集手机/邮箱**（最小收集），无相关字段可泄露。
- 前端代码无明文密码（已改 PBKDF2 哈希）；后端日志仅记非敏感字段。

## 安全

### 4. 认证授权与 Token ✅（已补齐即时失效）
- 过期：JWT 30 天。
- 刷新：新增 `POST /api/auth/refresh`（旧 token 有效期内签发新 token）；前端 `cloudRequest` 遇 401 自动刷新重试一次。
- 注销即时失效：✅ 新增 `POST /api/auth/logout` 将 token 的 `jti` 加入内存黑名单；登出后旧 token 立即 401。
- 改密即时失效：✅ JWT 携带 `token_version`（DB 新增列），改密成功后 `token_version+1`，该用户所有旧 token 全部失效。
- 管理员接口：项目**无管理员角色/接口**，不存在「普通用户调管理员接口」的越权面（说明性结论）。

### 5. 注入防护 ✅
- SQL：全部使用 better-sqlite3 参数化查询（`?` 占位），无字符串拼接 → 无 SQL 注入（代码级）。
- NoSQL：不适用（SQLite）。
- 命令注入：无执行系统命令的接口；静态服务器路径有 `StartsWith(root)` 目录穿越防护（代码级）。

### 6. 传输与配置 ✅（已补齐）
- HTTPS：新增 `FORCE_HTTPS=1` 时强制 301 跳转；生产建议 Nginx 终结 TLS（文档已说明）。
- 安全响应头：✅ 新增 `X-Content-Type-Options: nosniff`、`X-Frame-Options: DENY`、`Referrer-Policy: no-referrer`、`Permissions-Policy`。
- 密钥不硬编码：✅ 生产（`NODE_ENV=production`）无 `JWT_SECRET`（≥16 字符）时**拒绝启动**；开发环境有显式 `dev-only` 占位。
- 环境隔离：`.env` + `.gitignore` + 敏感文件拦截。

### 7. 限流防暴破 ✅（已补齐锁定）
- 接口速率限制：认证接口每 IP 每分钟 20 次（429）。
- 登录失败锁定：✅ 同一 IP+用户名连续失败 5 次锁定 15 分钟（423）。

### 8. 密码存储 ✅
- 后端 bcrypt cost 12；本地 PBKDF2（10 万次 SHA-256 + 随机盐）；绝不明文/MD5。

### 9. 依赖漏洞扫描 ⚠️（待服务器执行）
- 本机无 Node 无法执行 `npm audit`。已写入部署文档：`npm install && npm audit`，要求修复或说明高危 CVE 后上线。

## 性能

### 10. 内存 + 缓存 ✅
- 内存：7 轮操作堆内存 6.4–8.5MB 波动无泄漏（实测）。
- 缓存：无缓存层 → 击穿/穿透/雪崩不适用（无风险）；无缓存即与 DB 天然一致；带 `?v=` 资源已启用长缓存。

## 运维

### 11. 备份与恢复 ✅（已演练通过）
- 新增 `backup.ps1` / `restore.ps1`；实测「备份 → 删除 → 恢复」内容完全一致。
- 部署文档补充 EC2 备份命令与「至少演练一次恢复」要求。

## 合规

### 12. PIPL ✅（已补齐）
- 最小收集：仅用户名+密码哈希（无手机/邮箱）。
- 告知同意：✅ 注册需勾选《用户协议》与《隐私政策》（前端拦截 + 后端 `agreed` 校验）。
- 审计日志：✅ 新增 `data/audit.log`（JSON 行：时间/操作/用户名/IP），记录注册、登录、登录失败、登出、改密、刷新；不含任何密码/令牌。

## 本清单新增/修改文件

- `server.js`（安全响应头、token 黑名单 + token_version、登出/刷新接口、登录锁定、审计日志、PIPL 同意、生产密钥校验、HTTPS 跳转）
- `script.js`（401 自动刷新、登出吊销、注册同意校验、本地登录统一提示，`?v=20260836`）
- `index.html` / `style.css`（注册同意勾选 UI，`?v=20260822`）
- `backup.ps1` / `restore.ps1`（备份恢复）
- `DEPLOY_EC2.md`（安全清单、备份、PIPL 章节）

---

# 后端实测结果（2026-08-11，Node v24.19.0）

> 此前后端结论为「代码级审查」；用户安装 Node 后已**真实启动 Express + node:sqlite 完成全链路自动化实测**，`test-backend.js` 共 **31/31 用例全部通过**。

| 分组 | 用例 | 结果 |
|---|---|---|
| 注册 | 未勾选同意 400（PIPL）/ 弱密码 400 / 成功 201 / 重复 409 | ✅ |
| 登录 | 账号不存在统一提示 401 / 密码错误 401 / 成功 200 / **响应无密码字段** | ✅ |
| 注入 | SQL 注入、注释注入 均 401（参数化查询生效） | ✅ |
| 防暴破 | 连错 5 次锁定 423 / 认证接口限流 429（默认阈值） | ✅ |
| Token | 改密后旧 token 401 / 登出后 401 / 刷新被吊销 token 401 / 刷新续签 200 可用 | ✅ |
| IDOR | 伪造密钥 token 401 / **篡改载荷 id 的 token 401** | ✅ |
| 传输 | `X-Content-Type-Options` / `X-Frame-Options` 响应头 | ✅ |
| 配置 | `server.js`/`.env`/`package.json`/`start-server.ps1` 均 404 | ✅ |
| 合规 | 审计日志 17 行、无密码明文 | ✅ |

**依赖漏洞扫描**：`npm audit` → **0 vulnerabilities**。

**关键变更 — 数据库驱动**：`better-sqlite3`（原生模块，本机无 Python/C++ 工具链导致 `node-gyp` 编译失败）→ **Node 内置 `node:sqlite`（DatabaseSync）**，零原生编译依赖、同步 API 几乎一致；已同步：`package.json` engines ≥22.5、`DEPLOY_EC2.md` 安装要求、`server.js` 建表/补列顺序修正（先 CREATE 再 ALTER）。
